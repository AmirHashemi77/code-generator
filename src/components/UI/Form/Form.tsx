import { type FC, type ReactNode } from "react";
import style from "./form.module.css";

interface PropsType {
  children: ReactNode;
}

const Form: FC<PropsType> = () => {
  return <div></div>;
};
export default Form;
